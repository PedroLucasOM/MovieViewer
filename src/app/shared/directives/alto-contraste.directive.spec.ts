import { AltoContrasteDirective } from './alto-contraste.directive';

describe('AltoContrasteDirective', () => {
  it('should create an instance', () => {
    const directive = new AltoContrasteDirective();
    expect(directive).toBeTruthy();
  });
});
